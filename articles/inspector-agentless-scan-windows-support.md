---
title: "Amazon Inspector のエージェントレス EC2 スキャンが Windows に対応しました！"
emoji: "🔍"
type: "tech"
topics: ["aws", "inspector", "windows"]
published: true
publication_name: cscloud_blog
---

こんにちは、CSC の [CloudFastener](https://cloud-fastener.com/) というプロダクトで TAM のポジションで働いている平木です！

本日、Amazon Inspector のエージェントレス EC2 スキャンが Windows に対応するアップデートが発表されました。

https://aws.amazon.com/jp/about-aws/whats-new/2026/03/amazon-inspector-agentless-ec2-scanning-windows/

## 3 行まとめ

- エージェントレス EC2 スキャンが Windows OS に対応し、SSM Agent なしで脆弱性スキャンが可能になりました
- 検出結果が CVE 単位ではなく、Microsoft の KB（Knowledge Base）単位にまとめられた「KB ベースの検出結果」として提供されるようになりました
- 既存の CVE ベースの Windows 検出結果は自動的に KB 形式へ移行されるため、ユーザー側での追加作業は不要です

## エージェントレス EC2 スキャンとは

Amazon Inspector の EC2 スキャンには、以下の 2 つのスキャン方式があります。

| スキャン方式 | 前提条件 | スキャン頻度 |
| --- | --- | --- |
| エージェントベース | SSM Agent + Inspector SSM Plugin | 6 時間ごと |
| エージェントレス | なし（EBS スナップショットを使用） | 24 時間ごと |

エージェントレススキャンは、Inspector が対象インスタンスの **EBS スナップショットを自動作成**し、EBS Direct API 経由でソフトウェアインベントリを収集・評価する仕組みです。スキャン完了後、Inspector が作成したスナップショットは自動削除されます。

SSM Agent のインストールや管理が不要なため、エージェントのサポート外となる OS バージョンや、エージェント導入が制約されている環境でも脆弱性管理が実現できます。

## 今回のアップデート内容

### Windows のエージェントレススキャン対応

従来、エージェントレス EC2 スキャンの Windows 対応はありませんでした。今回のアップデートにより、**Windows インスタンスにも SSM Agent なしでスキャンが実行できる**ようになりました。

スキャン対象の例としては以下が含まれます。

- Windows OS のパッケージ
- WordPress
- Apache HTTP Server
- Python パッケージ
- Ruby gems

:::message
エージェントレススキャンの場合、スキャン頻度はエージェントベース（6 時間ごと）より緩やかな **24 時間ごと**となります。リアルタイム性の高い脆弱性管理が求められる環境では、引き続きエージェントベーススキャンの利用を検討してください。
:::

### KB ベースの検出結果

今回のアップデートで、Windows の検出結果の形式が変わりました。

従来は CVE 単位で検出結果が発行されていましたが、Windows の脆弱性は **1 つの Microsoft セキュリティパッチ（KB）で複数の CVE がまとめて修正**される特性があります。これを踏まえ、関連する CVE を 1 つの KB 単位にまとめた「**KB ベースの検出結果**」として提供されるようになりました。

KB ベースの検出結果には以下の情報が含まれます。

- そのパッチに含まれる CVE の中で**最も高い CVSS スコア**
- **EPSS スコア**（実際に悪用される確率）
- **エクスプロイトの可用性**情報
- **Microsoft KB 記事**への直接リンク

CVE が散在した状態で管理していた従来と比べ、**パッチ適用単位で優先度を判断**しやすくなり、実際の対応作業との整合性が高まります。

## 既存検出結果の自動移行

現在 Inspector で管理している既存の CVE ベースの Windows 検出結果は、**自動的に KB 形式へ移行**されます。ユーザー側での設定変更や手動対応は不要です。

## ハイブリッドスキャンモードとの関係

Amazon Inspector では、EC2 スキャンを有効化するとデフォルトで**ハイブリッドスキャンモード**が有効になります。このモードでは、SSM Agent が利用可能なインスタンスはエージェントベーススキャン、そうでないインスタンスはエージェントレススキャンが自動的に適用されます。

今回の対応により、Windows インスタンスにおいても SSM Agent が導入されていない場合にエージェントレススキャンにフォールバックする動作が期待できます。

:::message
ハイブリッドスキャンモードの詳細は以下をご参照ください。

https://docs.aws.amazon.com/inspector/latest/user/scanning-ec2.html
:::

## まとめ

今回のアップデートにより、Windows インスタンスの脆弱性管理がより柔軟かつ実践的になりました。

- **エージェントレス対応**によって、SSM Agent が導入できない環境でも Inspector による可視化が可能になりました
- **KB ベースの検出結果**によって、Windows のパッチ管理の実態に即した優先度判断ができるようになりました

既存の Windows スキャン結果は自動移行されるため、今すぐ確認して新しい検出形式に慣れておくことをおすすめします。

この記事がどなたかの役に立つと嬉しいです。
